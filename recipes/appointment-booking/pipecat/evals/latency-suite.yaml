concurrency: 1
runs_dir: eval-runs/latency
bots_dir: ..
scenarios_dir: audio
base_port: 7970
timeout: 10
spawn: "{python} {bot} -t eval --port {port}"
suite:
  - bot: eval_bot.py
    scenarios:
      - latency-reference
